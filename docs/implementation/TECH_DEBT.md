# TECH_DEBT.md

Per spec 47.157–47.158: no hidden shortcuts. This file tracks accepted debt visibly.

---

## Inherited from the old product (not created by this rebuild)

| Item | Description | Disposition |
|---|---|---|
| No `.env.example` ever existed | 33 env vars used across the old codebase with no documented list | Will not be carried forward — new, smaller var set gets a proper `.env.example` from Phase 1 |
| 31 loose, unordered SQL migration files | No migration tool, no guaranteed apply order, several tables defined twice with incompatible shapes | Superseded by Part 28 schema; old files kept in git history only, not executed |
| Zero automated tests, zero CI | Nothing in the old product is regression-tested | New build follows spec Part 32's testing pyramid from Phase 1 onward — this is now a P0 requirement (`REQUIREMENTS_MATRIX.md` row 75), not deferred debt |
| Real RLS/security gaps found in old schema (missing policies on 3 billing tables, public-read bug on `email_drip_log`, unscoped storage bucket policies) | Documented in `CURRENT_STATE_AUDIT.md` §3 | Not exploitable going forward since the old schema is not being deployed; documented for the record only |
| Anthropic SDK called directly from 6 old `(dashboard)` route files + `src/lib/ai/chatting.ts`, exactly what spec 5.2 forbids ("never scatter direct provider calls across the codebase") | Old product, untouched by the rebuild (still serves Production) | Not migrated; the new `src/lib/ai/gateway.ts` (Phase 7) is the only AI entry point for new-build features. Old routes stay as-is until the features they power are rebuilt or retired |

## Accepted in this rebuild

| Item | Description | Disposition |
|---|---|---|
| `fan_memories` has no version history table yet (spec 8.37: created/updated/confirmed/contradicted/expired should be historized) | Phase 6 vertical slice stores only current state; contradictions are overwritten (most recent wins), not journaled | Add `fan_memory_versions` when a feature actually needs the history (e.g. an audit view or a "why did this change" trace) |
| `fan_scores` has no history table (spec 9.9 implies score evolution should be traceable over time, not just the current value) | Same reasoning — one current row per fan, `version` int increments but old values aren't kept | Add score history once Analytics (Phase 12) or Benchmark needs a time series |
| AI analysis now runs automatically after every message (no Budget Guards yet — spec 5.29) | Owner explicitly required automatic analysis over a manual button; no per-agency/per-conversation rate limiting or cost cap exists yet, so a very chatty mock conversation calls the AI on every single message | Add budget guards (max analyses/conversation/hour, or debounce rapid-fire messages) before this runs against real, high-volume conversations — currently fine at mock-testing volume |
| ~~No live push for background AI updates~~ **Resolved**: `ConversationView` fixed-delay `router.refresh()` guesses (started at 3s/7s, stretched to 3/7/12/18s, still missed Full AI's slower decision task) replaced with a real Supabase Realtime subscription (`0015_realtime_inbox.sql` + `postgres_changes` on `messages` inserts / `conversations` updates) | Fixed-delay guessing was never going to keep up as prompts/tasks get slower — owner correctly pushed back ("il faut que ce soit instantané") | The Fan Intelligence panel (`fan_scores`/`fan_memories`) still uses a one-shot `router.refresh()` from the same action, not its own Realtime subscription — extend the same pattern there if it's ever seen to lag |
| AI Gateway has two model tiers (Fast/Haiku, Standard/Sonnet) but no escalation logic or fallback chain (spec 5.7, 5.11, 5.19) — a task always uses its one assigned model, never retries on a different one | Only 3 tasks exist so far; building tiering/escalation logic now would be scaffolding without enough data points to justify it | Revisit once more tasks exist and there's real signal on where a fixed model choice underperforms |
| No Benchmark / eval harness (spec Part 30) exists yet — prompt quality was only caught by the owner manually reading one test conversation, which is exactly how `memory-extraction-v1` shipped with the category-misuse bug (see BUILD_PROGRESS.md) | Part 30's full Benchmark/Gold Dataset machinery is explicitly a later phase, not Phase 7 | Before Phase 11 (Full AI) goes anywhere near real fans, a minimal eval set (a handful of hand-labeled mock conversations checked on every prompt change) should exist — flagged here so it isn't forgotten |
| Copilot has no Response Validator (spec 11.27 — price/media/script/permission/platform-rule checks before a suggestion is shown) | There is nothing to validate against yet: no Script Engine (Phase 9), no Pricing Engine/media catalog (Phase 10). The prompt itself instructs the AI to never invent prices/offers, which is a prompt-level guard, not a deterministic validator | Build the real Response Validator once Script/Pricing/Media engines exist and there are actual rules to check a suggestion against |
| No Duplicate Prevention or Repetition Detection (spec 11.37, 11.38) — nothing stops the AI from suggesting the same phrasing twice in a row, or a chatter double-sending | Out of scope for a single-suggestion-at-a-time MVP with a human always clicking Send | Add once Copilot sees real multi-conversation daily use and repetition actually shows up as a problem |
| Human Edit Tracking data (`copilot_suggestions.edit_distance`, spec 11.30) is recorded but nothing reads it yet — no quality dashboard, no feedback loop | Spec 11.30 itself says this data must not auto-train the model; it's meant to feed future analysis/Benchmark, not to be acted on immediately | Wire into Conversation Quality Metrics (spec 11.39) when Analytics (Phase 12) is built |
| Structured output isn't schema-validated (spec 5.13/11.28) — `parseJsonResponse()` only checks the text is valid JSON, not that it matches the expected shape (e.g. a missing field, wrong type). No retry-with-correction or fallback chain on validation failure | The two bugs already found (missing business context, unsupported `temperature` param) were both caught by a human reading the output, not by validation — this gap is real, not theoretical | Add a lightweight per-task schema check (even a hand-written type guard) before Phase 11 (Full AI) removes the human from the loop — a malformed decision must never reach a fan unreviewed |
| `ai_decisions.estimated_cost` is recorded per call but nothing surfaces it — spec 33's Milestone 3 gate explicitly requires "AI cost visible internally" | No internal dashboard exists yet (that's Phase 12 - Analytics) | Add a minimal cost view (even a raw `sum(estimated_cost) by decision_type` query on an admin page) before real pilot traffic, so a runaway cost isn't discovered by surprise |
| Script Engine (Phase 9) only has 2 node types (message, paid_media) — no CONDITION, WAIT, NEGOTIATION, HUMAN_APPROVAL, CUSTOM_REQUEST, RECOVERY node types from spec 13.7/28.39, and no Media Selector (spec 13.10) or dynamic pricing rules (spec 13.11) | The underlying schema (generic node/edge graph) supports adding these as new rows, not a rewrite — but the runtime engine (`advanceScriptRun`) only knows how to execute the 2 types it has | Add node types as the features they depend on land: WAIT needs a scheduler (not built), NEGOTIATION needs the Negotiation Engine (Phase 6 in spec 33's own numbering, later than Script Engine), HUMAN_APPROVAL needs a review queue |
| Script Builder has no graph validation beyond "has a real step" and "paid steps have a price" (spec 13.38's fuller list: broken references, impossible branches, no end path, infinite loops, permissions) | The linear-sequence builder (forms/cards, not a visual graph editor) makes most of those structurally impossible to create in the first place — e.g. you cannot reference a node that doesn't exist because branch targets are a dropdown of real nodes | Revisit once/if a freer graph editor (spec 13.34's future visual canvas) lets an agency build genuinely invalid graphs |
| No Script Analytics UI (spec 13.40-13.43: entries/conversion/revenue/branch distribution/Script Health Score) — the raw events are captured in `script_run_events`, nothing reads them | Analytics is explicitly Phase 12 | Build the dashboards once Analytics exists; the event data is already there and shouldn't need backfilling |
| Adaptive script messages (`generation_mode: 'adaptive'`, `SCRIPT_MESSAGE` task) reuse the same un-validated JSON parsing and no-retry behavior as the rest of the AI Gateway (see the structured-output row above) | Same root cause, not new to this feature | Covered by the same fix (schema validation before Phase 11) |
| Script step delays (`script_nodes.delay_seconds`, spec 11.10/28.38) have no real background scheduler — `checkDueScriptRuns()` only fires when `/inbox/[id]` happens to be loaded by a human. A due step with nobody looking at the conversation just sits there | Vercel Cron is the obvious real mechanism, but (a) Cron only runs against Production deployments, not Preview — useless for the environment we've actually been testing in — and (b) Hobby-tier Vercel plans cap Cron at once/day, too coarse for minute-scale delays anyway | Before trusting delays in real usage: confirm the Vercel plan supports frequent Cron, add a `/api/cron/...` route using `createAdminClient()` (already exists, used elsewhere) once on Production, and keep `checkDueScriptRuns()` as a redundant catch-up rather than removing it |
| Delay duration is a flat number the agency types in — no coherence check against what's actually being sent (e.g. a 2-minute video shouldn't be followed by a message 5 seconds later) | `media_assets.duration_seconds` now exists (Phase 10) but nothing populates or reads it yet — no metadata extraction on upload, no engine logic ties delay to it | Wire duration extraction (spec 14.13, "no LLM needed") on upload, then have the script builder suggest/require a delay proportional to it for that step |
| Upload only trusts the browser-provided MIME type (`file.type`) to classify image/video/audio — spec 14.48 explicitly warns "never trust the client-provided name or MIME type alone" | No server-side content sniffing (magic-byte check) or file-size cap exists yet. The upload path also moved fully client-side (browser → Supabase Storage directly, to work around Server Actions' 1MB body limit and Vercel's ~4.5MB function payload cap), so there's now no server hop to add a check to without introducing one on purpose | Add before opening uploads beyond the owner's own testing — either a size/type check in a Storage trigger/Edge Function, or a post-upload verification step before a media is marked `active` |
| No AI Media Selection/Ranking, semantic search, bundles, duplicate-sale awareness, or performance-based recommendations (spec 14.20-14.32, 14.46-14.47) — an agency (or script) always picks the exact media asset by hand | All of Media Intelligence's "choose the right content automatically" layer is explicitly later scope; this phase only had to prove a real, price-safe asset can be sold | Build once Full AI (Phase 11) or Copilot needs to suggest media autonomously rather than following a human-authored script |
| No Media Library search/sort/bulk actions UI (spec 14.38-14.40) — folders (`media_folders`, added after owner feedback) give basic organization, but `/media` is still one flat grid per folder with no search or bulk operations | Out of scope for validating the Pricing Validator itself | Add once an agency actually has enough media that scrolling a folder is a real problem |
| Pricing left blank at upload ("l'IA décidera du prix minimum") has no AI behind it yet — a media with no `minimum_price` just can't be used in a paid script step until a human sets one | Spec 15's AI-suggested-minimum pricing isn't built; this phase only unblocked the *data model* (nullable price) so upload isn't forced to invent a number | Build the actual suggestion (e.g. based on similar media's historical price/conversion) once there's enough sales data to make it meaningful, not before |
| Toggling a media to "gratuit / hors vente" (`is_for_sale = false`) clears its `target_price`/`minimum_price` outright rather than preserving them | Simplest correct behavior for now (a free asset shouldn't carry a stale price) but means re-enabling sale later requires re-entering both prices | Revisit if agencies report toggling back and forth often enough that losing the old price is annoying |
| Full AI (Phase 11) only supports 2 action types — `send_message` (plain reply) and `send_paid_offer` (one standalone-allowed, already-priced media, at its `target_price` with no negotiation) — not the full spec 47.91 set (`WAIT`, `FOLLOW_UP`) | Negotiation Engine, proactive Follow-ups, and AI Media Selection/Ranking don't exist yet (all separately tracked below/above) — Full AI can only act within what those other systems already provide, which today is "reply" and "sell one whitelisted item at its listed price" | Extend `FullAiDecisionResult`'s action union and the Action Validator together as each dependency (Negotiation Engine, Follow-up scheduler) actually lands — never add an action type the validator can't yet police |
| Full AI and the Script Engine can't run on the same conversation at once — `runFullAiDecision()` no-ops entirely if a Script Run is active, rather than the two systems coordinating | Building real coordination (spec 28.33's `START_SCRIPT`/`CONTINUE_SCRIPT` as Action Validator action types) is a bigger unification than this phase's exit criteria (spec 47.95) requires | Unify once a real use case needs both a human-authored script AND free-form Full AI reasoning on the same conversation — today an agency picks one or the other per conversation |
| Confidence thresholds for Full AI actions (`send_message: 0.7`, `send_paid_offer: 0.85` in `src/lib/ai/actionValidator.ts`) are fixed constants, not agency-configurable | Spec 10.8 anticipates per-action-type configurable thresholds; building that settings UI before there's any real signal on whether 0.7/0.85 are even reasonable defaults would be premature | Make configurable (likely alongside the kill-switch settings on `/settings/ai`) once real usage shows the fixed thresholds are wrong for some agencies |
| No automated benchmark/eval suite backs Full AI's "critical scenarios" (spec 47.95 points at Part 30's `ai_benchmark_cases`/`ai_benchmark_runs`/`ai_benchmark_results`, none of which exist) | Same gap already flagged for Copilot in Phase 7-8 — Part 30's full harness is later scope, and this phase's scenarios were instead validated by hand via the mock panel | Build the minimal hand-labeled eval set flagged back in Phase 7's tech debt row before this ever touches real pilot traffic — it now guards two autonomous-send paths (Copilot suggestions people can decline, and Full AI sends that go out unreviewed), not one |
| The "global OmniFlow kill switch" (spec 10.29's 4th level) has no admin UI — a row can only exist if inserted directly via SQL/service role | No admin backoffice exists yet (that's Phase 15) | Build a real internal control when Phase 15 (Admin Backoffice) is built; the schema/RLS/check logic already supports it today, only the internal-facing UI is missing |
| The "missed opportunity" deflection message and the escalation/notification behavior are hardcoded, not agency-configurable | Owner explicitly asked for this and similar Full AI behaviors to become per-agency settings eventually, but asked for the underlying behavior fix first — building a settings UI for one/two toggles before there's a second one to justify a shared pattern would be premature | When another Full AI behavior needs the same treatment, build a small "Full AI behavior" section on `/settings/ai` (deflection message tone/text, whether missed-opportunity notifications are on) covering both at once rather than one bespoke toggle at a time |
| `agency_notifications` has exactly 2 types (`escalation`, `missed_opportunity`) and only the `NotificationBell` reads them — no notification center/history page, no per-type mute, no email/push beyond the in-tab browser `Notification` API (which does nothing if the tab/browser isn't open) | Matches this phase's actual need; a full notification system (spec Part 38) is separate, later scope | Extend the `type` check + add a `/notifications` page once there's a second real use for it beyond Full AI, or wire a real push channel if "the tab must be open" proves too limiting |
| AI-attributed revenue (`/analytics`) only counts purchases traced through a real `offers` row from `script_node`/`full_ai` — a human chatter's manual sale, or a mock purchase with no matching offer, is never attributed to AI even if Copilot helped write the message that led to it | Spec 44.12 explicitly forbids arbitrary/guessed attribution; there's no reliable signal today linking "which message convinced the fan" to a later purchase for Copilot-assisted human sends | Revisit once there's a real attribution mechanism (e.g. tagging the purchase to the most recent `copilot_suggestions.status IN ('sent','edited_sent')` before it) — don't guess in the meantime |
| Copilot's "Regeneration Rate" (spec 44.32) isn't computable — `regenerateCopilotSuggestion()` and `discardCopilotSuggestion()` (manual "Écrire moi-même") both set `copilot_suggestions.status = 'discarded'`, so the two paths aren't distinguishable after the fact | Wasn't a distinction the schema needed before Analytics — `/analytics` shows a combined "discarded/regenerated" count instead of fabricating a split | Add a `resolution_method` column (or reuse a differently-named status) the next time this data is actually needed, e.g. once regeneration itself becomes a lever an agency wants to tune |
| `getCreatorComparison()`/`getFanSegments()` in `src/lib/analytics/metrics.ts` fetch full row sets (conversations, messages, fan_scores) and aggregate in application code rather than with a SQL `group by` | Fine at mock-data volume; keeps the metric logic simple and readable for a first pass | Move to real aggregation queries (or the pre-computed aggregation layer spec 44.67 anticipates) once an agency has enough fans/messages that this becomes slow |
| No AI cost visibility on `/analytics` yet, despite `ai_decisions.estimated_cost` now having a natural home there | Phase 12 focused on the P0/P1 KPI list (spec 44.101), which doesn't include cost; this was already tracked as a gap since Phase 7 | Add a "Coût IA" card/table to `/analytics` reading `sum(estimated_cost) by decision_type` — the data has existed since Phase 7, only the view is missing |
| No automated test confirms `/analytics` numbers match a mock scenario exactly (spec 47.100's literal exit bar) | No test suite exists yet anywhere in this rebuild (tracked since Phase 1) | Owner should hand-verify at least once before treating this phase as done; a real regression test can follow once `REQUIREMENTS_MATRIX.md` row 76's testing gap is addressed generally |
| No real payment provider is wired (`agencies.billing_provider = 'mock'` always) — subscriptions, plan changes, and invoices are all state flips in our own DB, no Stripe Checkout/Billing, no webhooks, no real card ever charged | Owner explicitly chose to stay Mock for this pass (no provider account set up yet); spec 22.61 itself calls this the correct approach for a dev/demo phase | Wire a real provider once the owner has an account — the entitlement model (`agencies.plan_id`/`status`) is already provider-agnostic, so this should mostly be swapping `changePlan()`'s mock update for a real Checkout session + webhook handler, not a redesign |
| Commission Base eligibility (`ELIGIBLE_TRANSACTION_TYPES` in `src/lib/billing/ledger.ts`) — which transaction types actually owe OmniFlow a commission — is a business-rule assumption made without legal/commercial sign-off | Spec 22.5 explicitly warns "ne pas implémenter une définition ambiguë sans validation commerciale/juridique"; a reasonable default was needed to build/test the ledger machinery at all | Get real sign-off on the eligible list before this ever touches a real invoice — flagged in `BILLING_GOLDEN_TESTS.md` too |
| Commission attribution (`transaction_attribution`) only distinguishes `full_ai`/`script`/`unknown` — never `copilot` or `human`, even though those are real spec 28.56 categories | Same root cause as Phase 12's AI-attributed revenue: no reliable signal today links "which message convinced the fan" back to a Copilot-assisted human send | Same fix as that Phase 12 debt row — solve both attribution gaps together once a real mechanism exists, don't build two divergent heuristics |
| Refunds, reversals, disputes, multi-currency, and duplicate-webhook idempotency are schema-ready (`transactions.status`, `commission_adjustments`) but have no application code exercising them yet | No real payment provider means no real refund/webhook-retry ever happens today | Build the handling when the real provider is wired — the schema (spec 28.53/28.59) was already sized for it so this shouldn't require new tables |
| Plan changes (`changePlan()`) are instant with no proration, no trial period, no grace period on failed payment, no cancellation flow (spec 22.37-22.45, 22.40-22.44 largely unbuilt) | Way out of scope for a first vertical slice whose job was proving the commission math is correct (spec 47.108) | Build subscription lifecycle management once a real provider exists to actually enforce it (most of this is meaningless against a Mock provider that can't fail a payment) |
| MYM's real connector (`src/lib/platforms/mymAdapter.ts`) is reverse-engineered access (session Bearer token), not an official API — the owner explicitly overrode spec 47.112's "No Illegal Shortcut" instruction for this after research confirmed no official API/partner program exists for MYM or OnlyFans anywhere in the market. Real, ongoing risk: MYM can change its internal endpoints or detect/block this pattern at any time with no notice, unlike an official, versioned API | This is a deliberate owner decision (`OPEN_QUESTIONS.md` Q2), not an oversight — documented so it isn't mistaken for one later | Monitor for breakage after any MYM web app update; if MYM ever ships an official partner API, migrate to it (the Adapter Contract in `src/lib/platforms/adapter.ts` is designed so swapping the implementation behind `mymAdapter` doesn't require touching any call site) |
| ~~`mymAdapter.fetchMessages()` always labels a synced message `direction: 'inbound'`~~ **Resolved**: the real `styx.mym.fans/v1/chats/{fanId}` response (confirmed live via the owner's DevTools) returns `sender_id`/`receiver_id` on every message. `MYMMessage` now carries both, and `fetchMessages()` derives direction by comparing `senderId` to the conversation's fan id (`externalConversationId`) instead of guessing | Fixed by live discovery, not by guessing the field name | None — watch for MYM ever restructuring this response shape (see the reverse-engineered-access row above) |
| `getConversations()`/`getMessages()` now call real, live-confirmed endpoints (`GET styx.mym.fans/v1/chats?limit=&last_message_opts=light` and `GET styx.mym.fans/v1/chats/{fanId}?limit=`) discovered via the owner's DevTools, replacing the old guessed `mym.fans/api/v2/...` URLs (which always 404'd) | ~~The auth header scheme for these two endpoints was assumed~~ **Resolved**: first live test 403'd; the owner's Request Headers capture confirmed `Authorization: Bearer <token>` was the right scheme all along, but `loginAndGetToken()` was returning Cognito's **IdToken** when MYM's real API expects the **AccessToken** (confirmed by the JWT's own `"token_use": "access"` claim visible in the captured header) — fixed to prefer `AccessToken` | None currently — if it still 403s after this fix, the next thing to check is whether `styx.mym.fans` also validates `Origin`/`Referer` (the real browser request sends `Origin: https://creators.mym.fans`, which a server-side fetch doesn't send by default) |
| `getConversations()`'s `last_message_opts=light` response mode doesn't include the last message's text, only its date/read state — `MYMConversation.lastMessage` is always `''` | Not needed by any current caller (`mymAdapter.fetchConversations()` only reads `lastMessageAt`); `getMessages()` is the real source of message content | Switch to `last_message_opts=full` (or a second call) only if something starts needing a conversation-list preview snippet |
| `getConversations()`/`getMessages()` only fetch a single page — the real API's `cursor` field (pagination token) is accepted as an optional param but nothing calls a second page automatically | A first-sync pass at moderate `limit` is enough to validate the connector end-to-end; older history beyond the first page isn't synced yet | Add cursor-following (loop until `cursor` is null/repeats) once validating against a creator with more history than one page covers |
| No unique constraint prevents two `platform_connections` rows for the same (creator, platform) beyond application-level `maybeSingle()` lookups in `getOrCreateMymConnection()`/sync code | Matches the existing table's original design (0004_conversations.sql already had a `unique(creator_id, platform_id)` constraint, so this is actually already enforced — flagged here only because it wasn't re-verified during this phase, not because it's missing) | No action needed unless testing reveals otherwise |
| Real *sending* through the MYM adapter (Copilot suggestions, Full AI, human replies, Script Engine actually reaching a real MYM conversation) is not wired — every existing send path (`sendHumanMessage`, `sendCopilotSuggestion`, `advanceScriptRun`, `fullAi.ts`) still only writes to our own `messages` table regardless of whether the conversation is Mock or a real MYM connection | Deliberately deferred: touching all 4 send call sites at once, across previously-tested Phases 5/8/9/11, in the same pass as building the connector itself was judged too much blast radius for one increment — flagged to the owner explicitly rather than bundled in | Build next, once read-sync is confirmed working live: likely a single `deliverOutboundMessage()` dispatcher (checks the conversation's platform, calls the adapter's `sendMessage()` when it's a real platform) that all 4 send paths call through, rather than duplicating platform-dispatch logic 4 times |
| No automated content/policy check gates Full AI from ever running on a real OnlyFans conversation — the "Copilot-only on OnlyFans" rule from `OPEN_QUESTIONS.md` Q1 exists only as documentation right now, not as code | The OnlyFans connector itself doesn't exist yet (queued behind MYM), so there's nothing to gate yet | When building the OnlyFans adapter, add a hard platform-level check in the Action Validator (`src/lib/ai/actionValidator.ts`) — not just a UI toggle — so Full AI physically cannot execute against an OnlyFans-platform conversation even if something upstream misconfigures it |
| `platform_credentials` stores the creator's actual MYM **email + password** (encrypted), not just a session token, after the owner asked for a simpler connect flow (paste a token from DevTools → just enter email/password) — `getMymCredentialsForCreator()` calls `loginAndGetToken()` fresh on every sync/send | A stored password is a wider blast radius if the encryption key is ever compromised than a stored session token would be (the creator may reuse that password elsewhere), but it avoids the connection silently going stale when a cached token expires, and matches what the owner explicitly asked for over token-pasting | If this becomes a real concern before real pilot traffic, switch to storing only the derived Bearer token plus a refresh-on-401 retry (re-login transparently only when a call actually fails auth) instead of storing the raw password — reduces at-rest sensitivity at the cost of that added complexity |
| `ENCRYPTION_KEY` (the AES-256-GCM key protecting all `platform_credentials` rows) was pasted in full into this chat session by the owner (screenshot of the Vercel dashboard) while asking an unrelated question | Owner was advised to rotate it immediately; whether they actually did is unverified as of this entry | Confirm the key was rotated (or deliberately left as-is because no real credentials existed yet at the time) before this connector is trusted with a real creator's real MYM password |
